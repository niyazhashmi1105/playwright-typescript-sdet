trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- task: UseNode@1
  inputs:
    version: '22'
  displayName: 'Install Node.js'
- task: JavaToolInstaller@0
  inputs:
    versionSpec: '17'
    jdkArchitectureOption: 'x64'
    jdkSourceOption: 'PreInstalled'
  displayName: 'Setup Java for Allure CLI'

- script: npm ci
  displayName: 'npm ci'
- script: npx playwright install --with-deps
  displayName: 'Install Playwright browsers'
- script: npm run test:smoke
  displayName: 'Run Playwright tests'
  env:
    CI: 'true'
- task: PublishTestResults@2
  displayName: 'Publish test results'
  inputs:
    searchFolder: 'junit-report'
    testResultsFormat: 'JUnit'
    testResultsFiles: 'junit-results.xml'
    mergeTestResults: true
    failTaskOnFailedTests: true
    testRunTitle: 'Junit Tests'
  condition: succeededOrFailed()
- task: PublishPipelineArtifact@1
  inputs:
    targetPath: playwright-report
    artifact: playwright-report
    publishLocation: 'pipeline'
  condition: succeededOrFailed()

- script: |
    npm install -g allure-commandline --save-dev
    npm install --save-dev allure-playwright
  displayName: 'Install Allure Commandline'
  condition: succeededOrFailed()
  
- task: PublishAllureReport@2
  displayName: 'Publish to Azure Pipelines Allure Tab'
  inputs:
    testResultsDir: allure-results
    reportName: 'Allure Results'
  condition: succeededOrFailed()

# - script: npm run allure
#   displayName: 'Generate Allure Report'
- task: PublishPipelineArtifact@1
  inputs:
    targetPath: allure-results
    artifact: allure-results
    publishLocation: 'pipeline'
  condition: succeededOrFailed()